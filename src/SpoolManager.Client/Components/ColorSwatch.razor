@if (!string.IsNullOrEmpty(ColorHex))
{
    <span class="color-swatch d-inline-block rounded border me-1"
          style="background-color: #@ColorHex; width: 1.2em; height: 1.2em; vertical-align: middle;"
          title="@("#" + ColorHex)"></span>
}
