@page "/imprint"

<PageTitle>@L["legal.imprint.title"] – @L["app.name"]</PageTitle>

<div class="container py-4" style="max-width:700px">
    <h1 class="mb-4">@L["legal.imprint.title"]</h1>

    @if (_loading)
    {
        <div class="text-center py-5"><div class="spinner-border text-secondary"></div></div>
    }
    else if (!string.IsNullOrWhiteSpace(_content))
    {
        @((MarkupString)_content)
    }
    else
    {
        <div class="alert alert-info">
            <i class="bi bi-info-circle me-2"></i>@L["legal.not.configured"]
        </div>
    }

    <hr class="mt-5" />
    <p><a href="/privacy">@L["legal.privacy.title"]</a></p>
</div>

